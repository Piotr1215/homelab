---
# ExternalSecret Template
# This template creates an ExternalSecret that fetches secrets from an external secret store
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{SECRET_NAME}}
  namespace: {{NAMESPACE|default:default}}
spec:
  refreshInterval: {{REFRESH_INTERVAL|default:1h}}
  secretStoreRef:
    name: {{SECRET_STORE_NAME|default:bitwarden-secretsmanager}}
    kind: {{SECRET_STORE_KIND|default:ClusterSecretStore}}
  target:
    name: {{SECRET_NAME}}
    creationPolicy: {{CREATION_POLICY|default:Owner}}
  data:
{{SECRET_DATA}}
