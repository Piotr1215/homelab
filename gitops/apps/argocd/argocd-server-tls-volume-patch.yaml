apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-server
  namespace: argocd
spec:
  template:
    spec:
      volumes:
      - name: tls-certs
        secret:
          secretName: argocd-server-backend-tls
