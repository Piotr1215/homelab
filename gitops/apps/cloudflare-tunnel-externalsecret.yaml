---
# ExternalSecret for Cloudflare Tunnel vCluster YAML
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: tunnel-credentials-vcluster-yaml
  namespace: cloudflare-tunnel
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-secretsmanager
    kind: ClusterSecretStore
  target:
    name: tunnel-credentials-vcluster-yaml
    creationPolicy: Owner
  data:
  - secretKey: credentials.json
    remoteRef:
      key: d7a29255-9bfb-4ffe-a1c5-b379013b42af
