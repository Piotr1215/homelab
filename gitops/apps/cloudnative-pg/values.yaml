# CloudNative-PG Operator
# Minimal config - operator only, clusters defined per-app
---
monitoring:
  podMonitorEnabled: true

# VPA recommendations (auto-synced)
resources:
  requests:
    cpu: 15m
    memory: 100Mi
  limits:
    cpu: 15m
    memory: 100Mi
