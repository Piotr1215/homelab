---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: r2r-repo-health
  namespace: ai-tools
  labels:
    app.kubernetes.io/name: r2r-repo-health
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  replicas: 1
  # Single replica + a stable LB IP: the exporter holds the last pushed health
  # snapshot in memory, so a second replica would answer /metrics with a stale
  # or empty snapshot depending on which pod is scraped.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: r2r-repo-health
  template:
    metadata:
      labels:
        app.kubernetes.io/name: r2r-repo-health
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: r2r-repo-health
          image: python:3.14-slim
          command: ["python", "/app/server.py"]
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: PORT
              value: "8080"
            # LAN + pod CIDR: with externalTrafficPolicy Cluster, kube-proxy
            # SNATs serval's push to a pod-network IP (10.233.x), so both the
            # LAN and the pod CIDR must be allowed. Real per-sender auth (shared
            # token) is deferred.
            - name: ALLOWED_PUSH_CIDRS
              value: "192.168.178.0/24,10.233.64.0/18"
            - name: PYTHONUNBUFFERED
              value: "1"
            - name: PYTHONDONTWRITEBYTECODE
              value: "1"
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 1000
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 30
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 60
          volumeMounts:
            - name: script
              mountPath: /app
      volumes:
        - name: script
          configMap:
            name: r2r-repo-health-script
---
apiVersion: v1
kind: Service
metadata:
  name: r2r-repo-health
  namespace: ai-tools
  labels:
    app.kubernetes.io/name: r2r-repo-health
  annotations:
    # Pin a static MetalLB IP so serval has a stable /push endpoint on the LAN.
    metallb.universe.tf/loadBalancerIPs: "192.168.178.95"
spec:
  type: LoadBalancer
  # Cluster, not Local: MetalLB L2 did not announce this static IP under Local
  # (ARP silent from serval, ping + curl to .95 timed out), while every other
  # homelab LB service uses Cluster and is reachable. Trade-off: with L2 +
  # Cluster, kube-proxy SNATs the push to a pod-network IP (verified 10.233.66.x,
  # inside 10.233.64.0/18), so the receiver never sees serval's real LAN IP. The
  # /push allowlist therefore admits both the LAN and the pod CIDR (see env);
  # real per-sender auth is deferred to a shared-token check on /push.
  externalTrafficPolicy: Cluster
  selector:
    app.kubernetes.io/name: r2r-repo-health
  ports:
    - port: 8080
      targetPort: http
      name: http
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: r2r-repo-health
  namespace: ai-tools
  labels:
    app.kubernetes.io/name: r2r-repo-health
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: r2r-repo-health
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
